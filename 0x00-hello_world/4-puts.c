#include <stdio.h>
/**
 * main - A programm that prints using the function puts
 * Return: 0 (success)
 */
int main (void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
