#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to mutiply with first number
 * Return: the result of multiplication between a et b
 */

int mul(int a, int b)
{
return (a * b);
}
